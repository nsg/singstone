# Native sherpa-onnx libraries must be supplied locally; setting this variable
# prevents sherpa-onnx-sys from downloading a prebuilt archive during a Cargo
# build. Snapcraft overrides the path with its source-built staging directory.
[env]
SHERPA_ONNX_LIB_DIR = { value = "target/native/lib", relative = true }
